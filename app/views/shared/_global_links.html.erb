<% if logged_in? %>
 	<strong><%= link_to current_user.site_name, profile_url(current_user) %> | 
	<%= link_to t('site.dashboard'), root_url %> | 
	<% if admin? -%><%= link_to t('site.admin'), admin_root_url %> | <% end -%>
	<%= link_to t('session.log_out'), destroy_user_session_path %>
<% else %>
	<%= link_to t('session.sign_up'), new_user_registration_path %> | 
	<%= link_to t('session.log_in'), new_user_session_path %>
<% end %>